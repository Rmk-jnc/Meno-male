const lightbox=document.querySelector('#lightbox');const boxImage=lightbox.querySelector('img');document.querySelectorAll('.photo-slot img').forEach(img=>img.addEventListener('error',()=>img.closest('.photo-slot').classList.add('missing')));document.querySelectorAll('.photo-slot').forEach(slot=>slot.addEventListener('click',()=>{if(slot.classList.contains('missing'))return;const img=slot.querySelector('img');boxImage.src=img.src;boxImage.alt=img.alt;lightbox.classList.add('open');lightbox.setAttribute('aria-hidden','false');document.body.style.overflow='hidden'}));function closeBox(){lightbox.classList.remove('open');lightbox.setAttribute('aria-hidden','true');document.body.style.overflow=''}lightbox.querySelectorAll('button').forEach(button=>button.addEventListener('click',closeBox));document.addEventListener('keydown',event=>{if(event.key==='Escape')closeBox()});document.querySelector('#year').textContent=new Date().getFullYear();
